import React, { Component } from 'react';

class Category extends Component {
    state = {  };
    
    render() { 
        return ( null );
    }
}
 
export default Category;

//this will be super simple, each category will be a clickable link with an image and 'title' text